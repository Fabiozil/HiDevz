const { getTimeStamp } = require("./index.js");

// test("Should get UTC time stamp", () => {
//     const timeStamp = getTimeStamp();
//     expect(timeStamp).toBe;
// });

test("Should be 3", () => {
    const add = 2 + 1;
    expect(add).toBe(3);
});
